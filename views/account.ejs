<!DOCTYPE html>
<html>
    <head>
        <meta http-equiv="Content-Type" content="text/html; charset=UTF-8" />
        <meta name="mobile-web-app-capable" content="yes" />
        <meta name="viewport" content="width=device-width, user-scalable=no, initial-scale=1.0, minimum-scale=1.0, maximum-scale=1.0" />
        <meta name="description" content="" />
        <meta name="author" content="" />
        <title>Account</title>
        <link rel="stylesheet" href="/static/css/custom.css" />
        <script src="/static/js/bg.js"></script>
    </head>
    <body>
        <div id="window">
            <div id="blur"></div>
            <div class="content center">
                <div class="error"><%= error %></div>
                <h2><span class="thinblock">Welcome </span><strong><%= user.getName() %></strong></h2>
                <ul name="character">
                    <% for(let character of user.getCharacters()) { %>
                        <li><img src="http://image.eveonline.com/Character/<%= character.id %>_32.jpg" alt="<%= character.name %>" /><span><%= character.name %></span></li>
                    <% } %>
                </ul>
                <div class="form-actions"><form class="half" action="/account/add"><button type="submit" >ADD</button></form><form class="half" action="/logout"><button type="submit" >LOGOUT</button></form></div>
            </div>
        </div>
    </body>
</html>






