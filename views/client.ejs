
    "use strict";

    <% for(var name in definitions) { %>
        class <%= name %> {

            constructor () {

            }

        }

        <%= name %>.types = {
            <% for(var key in definitions[name].properties) { %>
                <%= key %>: <%
                    var type = "";
                    if(definitions[name].properties[key].$ref) {
                        type = definitions[name].properties[key].$ref.split("/").slice(-1)[0];
                    } else {
                        switch (definitions[name].properties[key].type) {
                            case "number":
                            case "integer":
                                type = "Number"; break;
                            case "string":
                                type = "String"; break;
                            case "array":
                                type = "Array"; break;
                            default:
                                type = "Object"; break;
                        }
                    }
              %><%= type %>,
            <% } %>
        };
    <% } %>

    class SwaggerClient {

    }

    <%
        var handler = {};
        for(var path in paths) {
            for(var method in paths[path]) {
                handler[paths[path][method].operationId.split("_")[0]][paths[path][method].operationId.split("_")[1]] =
                    `
                        () {
                            return null;
                        }
                    `;
            }
        }
    %>
    <% for(var handlername in handler) { %>
        class <%= handlername %> {
        <% for(var methodname in handler[handlername]) { %>
            <%= methodname %> <%= handler[handlername][methodname] %>
        <% } %>
        }
    <% } %>

    if(typeof(module) != "undefined")
        module.exports = SwaggerClient;
